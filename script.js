const imageUpload = document.getElementById('imageUpload');
const preview = document.getElementById('preview');
const resizeWidth = document.getElementById('resizeWidth');
const resizeHeight = document.getElementById('resizeHeight');
const filterSelect = document.getElementById('filterSelect');
const frameDuration = document.getElementById('frameDuration');
const infiniteLoop = document.getElementById('infiniteLoop');
const createGifButton = document.getElementById('createGif');

createGifButton.addEventListener('click', () => {
  // ... (Image loading, modification, and GIF generation logic - same as before) 
});

// ... (Rest of the code from the previous examples)
